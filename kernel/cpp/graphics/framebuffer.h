#ifndef FRAMEBUFFER_CPP_H
#define FRAMEBUFFER_CPP_H

#include "../../c/graphics/framebuffer.h"

// In C++, we can extend the struct with methods if we want, 
// but it's cleaner to keep them separate if they clash.
// Since the C header already defines Framebuffer as a typedef of an anonymous struct,
// we can't easily redeclare it. 

#endif